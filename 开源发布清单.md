# GitHub 开源发布清单

## 发布前必须完成

- [ ] 移除或忽略 `seen_item_history.json`、翻译缓存、Google News 缓存、健康日志、日报和内部备查。
- [ ] 检查全文搜索结果，确认没有 API key、邮箱、Cookie、代理地址或个人路径。
- [ ] 用 `config.example.json` 代替本机配置；真实配置只放在未跟踪文件或环境变量中。
- [ ] 运行编译检查、32 项回归测试和一次 `--replay` 精确重放。
- [ ] 在 README 中说明普通深度分析、顶级报告、学术论文和长期研究的不同时间口径。
- [ ] 为每个高价值出版方说明来源类型、合法公开入口、访问限制和备用路径。
- [ ] 明确项目不绕过登录、付费墙、验证码和访问控制。
- [x] 已选择 Apache License 2.0；发布前仍需确认代码和文档均由版权持有人授权。

## 推荐的仓库结构

```text
digest_generator.py
digest_core/
  runtime.py
  http_client.py
  extraction.py
  state.py
_test_source_diversity.py
config.example.json
requirements.txt
README.md
CONTRIBUTING.md
SECURITY.md
```

## 发布后治理

- 用 issue 模板收集新来源、失效来源、解析错误和质量误收录。
- 定期查看 `source_health_YYYY-MM-DD.log` 和 `selection_audit_YYYY-MM-DD.json`，但不要把它们提交进仓库。
- 以出版方为单位维护适配器，主流程只负责编排、门禁、去重、审计和渲染。
